{% extends 'base.html.twig' %}

{% block title %}Libro{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="display-4">Detalles del Libro</h1>
        </div>

        <div class="card shadow-lg mb-4">
            <div class="card-body">
                <table class="table table-striped">
                    <tbody>
                        <tr>
                            <th scope="row">Id</th>
                            <td>{{ libro.id }}</td>
                        </tr>
                        <tr>
                            <th scope="row">Nombre</th>
                            <td>{{ libro.nombre }}</td>
                        </tr>
                        <tr>
                            <th scope="row">Género</th>
                            <td>{{ libro.genero }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="row">
            <div class="col">
                <a href="{{ path('app_libro_index') }}" class="btn btn-secondary btn-lg btn-block">Volver a la lista</a>
            </div>
            <div class="col">
                <a href="{{ path('app_libro_edit', {'id': libro.id}) }}" class="btn btn-warning btn-lg btn-block">Modificar</a>
            </div>
        </div>

        <div class="btn btn-danger mt-2">
            {{ include('libro/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
