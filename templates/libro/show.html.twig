{% extends 'base.html.twig' %}

{% block title %}Libro{% endblock %}

{% block body %}
    <h1>Libro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ libro.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ libro.nombre }}</td>
            </tr>
            <tr>
                <th>Genero</th>
                <td>{{ libro.genero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_libro_index') }}">volver a la lista</a>

    <a href="{{ path('app_libro_edit', {'id': libro.id}) }}">Modificar</a>

    {{ include('libro/_delete_form.html.twig') }}
{% endblock %}
