{% extends 'base.html.twig' %}

{% block title %}Registrar Libro{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="display-4">Registrar Nuevo Libro</h1>
        </div>

        <div class="card shadow-lg mb-4">
            <div class="card-body">
                {{ include('libro/_form.html.twig') }}
            </div>
        </div>

        <div class="mt-4">
            <a href="{{ path('app_libro_index') }}" class="btn btn-secondary btn-lg">Volver a la lista</a>
        </div>
    </div>
{% endblock %}
