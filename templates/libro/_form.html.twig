{{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}

    <div class="form-group row">
        <label for="{{ form.nombre.vars.id }}" class="col-sm-2 col-form-label">Nombre:</label>
        <div class="col-sm-10">
            {{ form_widget(form.nombre, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="form-group row">
        <label for="{{ form.genero.vars.id }}" class="col-sm-2 col-form-label">Género:</label>
        <div class="col-sm-10">
            {{ form_widget(form.genero, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="form-group row">
        <label for="{{ form.autorNombre.vars.id }}" class="col-sm-2 col-form-label">Autor:</label>
        <div class="col-sm-10">
            {{ form_widget(form.autorNombre, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="form-group row">
        <div class="offset-sm-2 col-sm-10">
            <button type="submit" class="btn btn-primary">{{ button_label|default('Guardar') }}</button>
        </div>
    </div>

{{ form_end(form) }}
