{% extends 'base.html.twig' %}

{% block title %}Nuevo Autor{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="display-4">Registro de Autor</h1>
        </div>

        <div class="card shadow-lg mb-4">
            <div class="card-body">
                {{ include('autor/_form.html.twig', {'button_label': 'Guardar'}) }}
            </div>
        </div>

        <div class="mt-4">
            <a href="{{ path('app_autor_index') }}" class="btn btn-secondary btn-lg">Volver a la lista</a>
        </div>
    </div>
{% endblock %}
